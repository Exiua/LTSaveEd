@using LTSaveEd.Models.CharacterData

<MudCheckBox @bind-Value="@Perk.Active" LabelPlacement="Placement.Start" Label="@Perk.DisplayName"/>

@code {
    
    /// <summary>
    ///     The PerkNode to bind to.
    /// </summary>
    [Parameter] public required PerkNode Perk { get; set; }
}